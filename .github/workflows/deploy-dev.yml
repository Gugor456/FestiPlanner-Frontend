name: Deploy Frontend (DEV)

on:
  push:
    branches: [develop]

jobs:
  deploy:
    environment: dev
    uses: ./.github/workflows/deploy-frontend.yml
    with:
      image_name: festiplanner-frontend-dev:${{ github.sha }}
      container_name: festiplanner-frontend-dev
      port: "8080"